spring:
  application:
    name: syllabussync
  
  profiles:
    active: development
  
  datasource:
    # Use H2 for development (free, embedded database)
    url: jdbc:h2:mem:syllabussync;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: 
    driver-class-name: org.h2.Driver
    # Uncomment for PostgreSQL in production
    # url: jdbc:postgresql://localhost:5432/syllabussync
    # username: syllabussync
    # password: syllabussync_password
    # driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
        use_sql_comments: true
    open-in-view: false
  
  h2:
    console:
      enabled: true
      path: /h2-console
  
  flyway:
    enabled: false
    
  sql:
    init:
      mode: never
  
# Google OAuth2 configuration
# Get these from Google Cloud Console:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Google Calendar API
# 4. Create OAuth 2.0 credentials
# 5. Add the same URI under OAuth client "Authorized redirect URIs" in Google Cloud Console
google:
  client-id: ${GOOGLE_CLIENT_ID}
  client-secret: ${GOOGLE_CLIENT_SECRET}
  redirect-uri: ${GOOGLE_REDIRECT_URI:http://localhost:8080/api/syllabus/auth/google/callback}
  # Generative AI: Vertex when GOOGLE_CLOUD_PROJECT is set; else consumer Gemini API with GEMINI_API_KEY.
  # Binds to google.gemini.* / google.vertex.* (see GenerativeAiClient).
  gemini:
    api-key: ${GEMINI_API_KEY:}
    model: ${GEMINI_MODEL:gemini-2.5-flash}
    max-tokens: 4096
  vertex:
    project-id: ${GOOGLE_CLOUD_PROJECT:}
    location: ${VERTEX_AI_LOCATION:us-central1}
    # Optional: GCP API key (Credentials → API key, restricted to Vertex AI). Uses key= on the request; no OAuth needed.
    api-key: ${VERTEX_API_KEY:${GOOGLE_API_KEY:}}

# Supabase — the source of truth for user identity. The backend verifies
# Supabase-issued JWTs using the project's JWT secret (HS256). Set
# SUPABASE_JWT_SECRET from Supabase Dashboard → Settings → API → JWT Secret.
supabase:
  url: ${SUPABASE_URL:}
  jwt-secret: ${SUPABASE_JWT_SECRET:}

# JWT Configuration
jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION:1800000} # 30 minutes in milliseconds
  
  mail:
    # Using free Gmail SMTP (requires app password)
    host: ${SMTP_HOST:smtp.gmail.com}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USERNAME:}
    password: ${SMTP_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  
  cache:
    type: simple  # Using simple cache instead of Redis
    # Redis configuration (optional for production)
    # type: redis
    # redis:
    #   host: ${REDIS_HOST:localhost}
    #   port: ${REDIS_PORT:6379}
    #   password: ${REDIS_PASSWORD:}
    #   timeout: 2000ms
    #   lettuce:
    #     pool:
    #       max-active: 8
    #       max-idle: 8
    #       min-idle: 0

# Server configuration
server:
  address: 0.0.0.0
  port: ${PORT:8080}
  servlet:
    context-path: /
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# Management endpoints
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized
  info:
    env:
      enabled: true

# Logging configuration
logging:
  level:
    com.syllabussync: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: ${LOG_FILE:./logs/syllabussync.log}

# Application specific configuration
app:
  jwt:
    secret: ${JWT_SECRET}
    expiration: 86400000 # 24 hours
  
  file:
    upload-dir: ${UPLOAD_DIR:./uploads}
    max-size: ${MAX_FILE_SIZE:10MB}
    allowed-types: pdf,docx,txt
  
  auth:
    # When true, every request must carry a valid Supabase JWT in the
    # Authorization header (see SupabaseJwtFilter). Production should flip
    # this on once SUPABASE_JWT_SECRET is set.
    required: ${APP_AUTH_REQUIRED:false}

  cors:
    # CSV list of allowed origins. Default "*" is convenient locally but
    # production should lock this down to the Vercel + preview hostnames.
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:*}

  oauth:
    # Fallback secret used to sign the Google OAuth `state` param when no
    # Supabase JWT secret is configured (dev only). In production we reuse
    # SUPABASE_JWT_SECRET so there's nothing extra to rotate.
    state-secret: ${APP_OAUTH_STATE_SECRET:syllabussync-local-oauth-state-secret-change-me-please-1234567890}

  google:
    calendar:
      timezone: America/New_York
      # Browser redirect after Google OAuth - now lands on the Settings
      # page so the signed-in user sees the freshly-connected state.
      oauth-success-redirect: ${CALENDAR_OAUTH_SUCCESS_URL:http://localhost:3000/settings}
      default-reminders:
        - days: 7
        - days: 3
        - days: 1
  
  syllabus:
    parsing:
      timeout: 30000 # 30 seconds
      max-file-size: 10485760 # 10MB
      supported-formats: pdf,docx,txt
  
  notification:
    email:
      enabled: true
      from: ${MAIL_FROM:noreply@syllabussync.com}
      subject-prefix: [SyllabusSync]
    reminder:
      enabled: true
      default-days: 7,3,1

---
# Development profile
spring:
  config:
    activate:
      on-profile: development
  
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  
  h2:
    console:
      enabled: true

logging:
  level:
    com.syllabussync: DEBUG
    org.springframework.web: DEBUG

---
# Production profile (Supabase Postgres: set SPRING_DATASOURCE_* to the pooler connection string; add ?sslmode=require if required)
spring:
  config:
    activate:
      on-profile: production

  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0
    repair-on-migrate: true

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false

  h2:
    console:
      enabled: false

logging:
  level:
    com.syllabussync: INFO
    org.springframework: WARN
    org.hibernate: WARN

---
# Docker profile
spring:
  config:
    activate:
      on-profile: docker
  
  datasource:
    url: jdbc:postgresql://postgres:5432/syllabussync
    username: syllabussync
    password: ${POSTGRES_PASSWORD:syllabussync_password}
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate

  cache:
    redis:
      host: redis
      port: 6379
